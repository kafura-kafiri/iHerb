templates best practice to lan > done
realise() <<

sign in sign out to header.

profile + wish_list

reviews {
    add
    delete
    ...
}
realise()

11c.js 9 matches .pagination -> .xpagination
git fetch --all
git reset --hard origin/master

mongo --quiet --eval 'db.getMongo().getDBNames().forEach(function(i){db.getSiblingDB(i).dropDatabase()})'

python3 -h http.server <-

mongodump
mongorestore

tar -cvzf _.tar.gz /